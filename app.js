const zero = document.querySelector("#zero")
const display = document.querySelector(".display")

display.innerHTML=`hi`;
zero.addEventListener("onclick" ,function() {
value
    const btnZero = zero.value;
    display.innerHTML=btnZero.value;

} )